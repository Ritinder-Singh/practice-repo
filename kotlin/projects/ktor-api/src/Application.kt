// PROJECT: Ktor REST API Application Entry Point

// TODO 1: Add Ktor imports once Gradle setup is complete:
//   import io.ktor.server.engine.*
//   import io.ktor.server.netty.*
//   import io.ktor.server.application.*
//   import io.ktor.server.plugins.contentnegotiation.*
//   import io.ktor.serialization.kotlinx.json.*

// TODO 2: Configure the application:
//   fun main() {
//       embeddedServer(Netty, port = 8080, module = Application::module).start(wait = true)
//   }

// TODO 3: Application module extension function:
//   fun Application.module() {
//       install(ContentNegotiation) { json() }
//       install(StatusPages) { exception<Throwable> { call, cause -> ... } }
//       configureRouting()
//   }

fun main() {
    println("TODO: start Ktor server")
}

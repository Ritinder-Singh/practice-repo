// TOPIC: Array Problems (LeetCode) | swift arrays.swift
// Run: swift arrays.swift

// TODO 1: LC #1 — Two Sum
// Given an array of integers and a target, return indices of two numbers that add to target.
// func twoSum(_ nums: [Int], _ target: Int) -> [Int]

// TODO 2: LC #121 — Best Time to Buy and Sell Stock
// Find the maximum profit from one buy and one sell (buy before sell).
// func maxProfit(_ prices: [Int]) -> Int

// TODO 3: LC #238 — Product of Array Except Self
// Return array where each element is the product of all others; no division, O(n).
// func productExceptSelf(_ nums: [Int]) -> [Int]

// TODO 4: LC #11 — Container With Most Water
// Two-pointer approach; find two lines that form container holding the most water.
// func maxArea(_ height: [Int]) -> Int

// TODO 5: LC #53 — Maximum Subarray
// Kadane's algorithm; find contiguous subarray with the largest sum.
// func maxSubArray(_ nums: [Int]) -> Int

// TODO 6: LC #56 — Merge Intervals
// Sort intervals by start, merge overlapping ones, return merged list.
// func merge(_ intervals: [[Int]]) -> [[Int]]

// TOPIC: Loops & Control Flow | swift 03_loops_control_flow.swift
// Docs: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/controlflow/

// TODO 1: for-in — iterate array, dictionary, range (1...5, 1..<5, stride)
// TODO 2: while & repeat-while — find first prime above N
// TODO 3: switch — exhaustive matching, ranges, tuples, value binding, where clause
// TODO 4: guard — early exit, bind optional in guard let, else must exit scope
// TODO 5: if-let chaining — if let a = opt1, let b = opt2, a > b { ... }
// TODO 6: labeled statements — outer: for i in 0..<10 { inner: for j in 0..<10 { break outer } }
// TODO 7: Pattern matching — case let x where x > 0, enum case matching in if/switch
// TODO 8: defer — cleanup code that runs on scope exit, multiple defers (LIFO order)

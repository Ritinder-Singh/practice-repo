// =============================================================================
// React App — Root Component
// =============================================================================
// TODO 1: React Router setup
//   / → ProductListPage | /products/:id → ProductDetailPage | /cart → CartPage
// TODO 2: React Query provider (QueryClient, QueryClientProvider)
// TODO 3: CartContext provider wrapping the whole app
// =============================================================================

// import { BrowserRouter, Routes, Route } from "react-router-dom";
// import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
// import { CartProvider } from "./store/CartContext";

export default function App() {
  return (
    <div>
      {/* TODO: add providers and router */}
      <h1>React + TypeScript App</h1>
    </div>
  );
}

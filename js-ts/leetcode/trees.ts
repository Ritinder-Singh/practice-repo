// =============================================================================
// TypeScript LeetCode — Trees
// =============================================================================

class TreeNode {
  constructor(public val=0, public left: TreeNode|null=null, public right: TreeNode|null=null) {}
}

// LC #226 Invert Binary Tree
// function invertTree(root: TreeNode|null): TreeNode|null { return null; }

// LC #104 Maximum Depth
// function maxDepth(root: TreeNode|null): number { return 0; }

// LC #572 Subtree of Another Tree
// function isSubtree(root: TreeNode|null, sub: TreeNode|null): boolean { return false; }

// LC #102 Level Order Traversal
// function levelOrder(root: TreeNode|null): number[][] { return []; }

// LC #98  Validate BST
// function isValidBST(root: TreeNode|null): boolean { return false; }

// LC #230 Kth Smallest in BST
// function kthSmallest(root: TreeNode|null, k: number): number { return 0; }

// LC #297 Serialize/Deserialize
// class Codec {
//   serialize(root: TreeNode|null): string { return ""; }
//   deserialize(data: string): TreeNode|null { return null; }
// }

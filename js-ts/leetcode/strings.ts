// =============================================================================
// TypeScript LeetCode — Strings
// =============================================================================

// LC #242 Valid Anagram
// function isAnagram(s: string, t: string): boolean { return false; }

// LC #125 Valid Palindrome
// function isPalindrome(s: string): boolean { return false; }

// LC #5   Longest Palindromic Substring
// function longestPalindrome(s: string): string { return ""; }

// LC #3   Longest Substring Without Repeating Chars
// function lengthOfLongestSubstring(s: string): number { return 0; }

// LC #424 Longest Repeating Character Replacement
// function characterReplacement(s: string, k: number): number { return 0; }

// LC #76  Minimum Window Substring
// function minWindow(s: string, t: string): string { return ""; }

// LC #49  Group Anagrams
// function groupAnagrams(strs: string[]): string[][] { return []; }

// LC #271 Encode/Decode Strings
// function encode(strs: string[]): string { return ""; }
// function decode(s: string): string[] { return []; }

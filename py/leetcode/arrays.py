# =============================================================================
# Python LeetCode — Arrays  (Blind 75 / NeetCode 150)
# Run: python arrays.py
# =============================================================================
from typing import List

# LC #1   Two Sum — O(n) hash map
# def two_sum(nums: List[int], target: int) -> List[int]: pass

# LC #121 Best Time to Buy/Sell Stock — O(n) single pass
# def max_profit(prices: List[int]) -> int: pass

# LC #238 Product Except Self — O(n), no division
# def product_except_self(nums: List[int]) -> List[int]: pass

# LC #53  Maximum Subarray — Kadane's O(n)
# def max_subarray(nums: List[int]) -> int: pass

# LC #152 Maximum Product Subarray — track max+min
# def max_product(nums: List[int]) -> int: pass

# LC #153 Min in Rotated Sorted Array — O(log n)
# def find_min(nums: List[int]) -> int: pass

# LC #33  Search in Rotated Array — O(log n)
# def search(nums: List[int], target: int) -> int: pass

# LC #15  3Sum — sort + two pointer O(n²)
# def three_sum(nums: List[int]) -> List[List[int]]: pass

# LC #11  Container With Most Water — two pointer O(n)
# def max_area(height: List[int]) -> int: pass

# LC #42  Trapping Rain Water — two pointer O(n)
# def trap(height: List[int]) -> int: pass

# =============================================================================
# Python LeetCode — Strings  (Blind 75 / NeetCode 150)
# =============================================================================
from typing import List

# LC #242 Valid Anagram — Counter O(n)
# def is_anagram(s: str, t: str) -> bool: pass

# LC #125 Valid Palindrome — two pointer O(n)
# def is_palindrome(s: str) -> bool: pass

# LC #5   Longest Palindromic Substring — expand around center O(n²)
# def longest_palindrome(s: str) -> str: pass

# LC #3   Longest Substring Without Repeating — sliding window O(n)
# def length_of_longest_substring(s: str) -> int: pass

# LC #424 Longest Repeating Character Replacement — sliding window O(n)
# def character_replacement(s: str, k: int) -> int: pass

# LC #76  Minimum Window Substring — sliding window O(n)
# def min_window(s: str, t: str) -> str: pass

# LC #49  Group Anagrams — sorted key O(n*k*log k)
# def group_anagrams(strs: List[str]) -> List[List[str]]: pass

# LC #271 Encode/Decode Strings — length prefix
# def encode(strs: List[str]) -> str: pass
# def decode(s: str) -> List[str]: pass

# LC #128 Longest Consecutive Sequence — set O(n)
# def longest_consecutive(nums: List[int]) -> int: pass

// =============================================================================
// Rust LeetCode — Arrays
// =============================================================================
fn main() { println!("Rust arrays leetcode"); }

// LC #1   Two Sum — hash map O(n)
// fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> { vec![] }

// LC #121 Best Time to Buy/Sell Stock — min track O(n)
// fn max_profit(prices: Vec<i32>) -> i32 { 0 }

// LC #238 Product Except Self — prefix+suffix O(n)
// fn product_except_self(nums: Vec<i32>) -> Vec<i32> { vec![] }

// LC #53  Maximum Subarray — Kadane's O(n)
// fn max_sub_array(nums: Vec<i32>) -> i32 { 0 }

// LC #15  3Sum — sort+two-pointer O(n²)
// fn three_sum(nums: Vec<i32>) -> Vec<Vec<i32>> { vec![] }

// LC #11  Container With Most Water — two pointer O(n)
// fn max_area(height: Vec<i32>) -> i32 { 0 }

// LC #42  Trapping Rain Water — two pointer O(n)
// fn trap(height: Vec<i32>) -> i32 { 0 }

// LC #56  Merge Intervals — sort+merge O(n log n)
// fn merge(intervals: Vec<Vec<i32>>) -> Vec<Vec<i32>> { vec![] }

// =============================================================================
// Go LeetCode — Arrays
// =============================================================================
package main
import "fmt"
// LC #1   Two Sum                     — hash map O(n)
// func twoSum(nums []int, target int) []int { return nil }
// LC #121 Best Time to Buy/Sell Stock — single pass min track
// func maxProfit(prices []int) int { return 0 }
// LC #238 Product Except Self         — prefix+suffix O(n)
// func productExceptSelf(nums []int) []int { return nil }
// LC #53  Maximum Subarray            — Kadane's O(n)
// func maxSubArray(nums []int) int { return 0 }
// LC #15  3Sum                        — sort+two-pointer O(n²)
// func threeSum(nums []int) [][]int { return nil }
// LC #11  Container With Most Water   — two pointer O(n)
// func maxArea(height []int) int { return 0 }
// LC #42  Trapping Rain Water         — two pointer O(n)
// func trap(height []int) int { return 0 }
// LC #56  Merge Intervals             — sort+merge O(n log n)
// func merge(intervals [][]int) [][]int { return nil }
func main() { fmt.Println("Go arrays leetcode") }

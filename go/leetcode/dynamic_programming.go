// =============================================================================
// Go LeetCode — Dynamic Programming
// =============================================================================
package main
import "fmt"
// LC #70  Climbing Stairs      — dp[i]=dp[i-1]+dp[i-2]
// func climbStairs(n int) int { return 0 }
// LC #198 House Robber         — dp[i]=max(dp[i-1], dp[i-2]+nums[i])
// func rob(nums []int) int { return 0 }
// LC #322 Coin Change          — bottom-up O(amount*coins)
// func coinChange(coins []int, amount int) int { return -1 }
// LC #300 LIS                  — O(n²) dp / O(n log n) patience sort
// func lengthOfLIS(nums []int) int { return 0 }
// LC #1143 LCS                 — 2D dp O(m*n)
// func longestCommonSubsequence(t1, t2 string) int { return 0 }
// LC #62  Unique Paths         — dp[i][j]=dp[i-1][j]+dp[i][j-1]
// func uniquePaths(m, n int) int { return 0 }
// LC #72  Edit Distance        — insert/delete/replace
// func minDistance(w1, w2 string) int { return 0 }
func main() { fmt.Println("Go DP leetcode") }

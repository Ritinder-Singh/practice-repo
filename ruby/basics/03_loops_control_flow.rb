# TOPIC: Loops & Control Flow | ruby 03_loops_control_flow.rb

# TODO 1: times, upto, downto — 5.times { |i| puts i }, 1.upto(10) { |i| }
# TODO 2: each, map, select, reject, reduce — array transformations
# TODO 3: while, until, loop — loop { break if condition }
# TODO 4: case/when — case value when 1..10 then ... when String then ... else ...
# TODO 5: Ternary and inline if/unless — x = y > 0 ? "pos" : "neg"; puts msg if debug
# TODO 6: next, break, redo — inside iterators; break with value: result = [1,2,3].each { |x| break x if x > 1 }
# TODO 7: Lazy enumerators — (1..Float::INFINITY).lazy.select(&:odd?).first(5)
# TODO 8: Enumerator::new — custom external iterator with Fiber-based suspension
